# Configuración para la documentación online:
# https://gatovid-api.readthedocs.io/es/latest/
#
# Más sobre el formato:
# https://docs.readthedocs.io/en/stable/config-file/v2.html

# Required
version: 2

# Build documentation in the docs/ directory with Sphinx
sphinx:
   configuration: docs/conf.py

python:
  version: 3.8
  install:
    - method: pip
      path: .
      extra_requirements:
        - docs
