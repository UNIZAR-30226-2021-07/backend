setup:
    config:
        - DEBUG: False
build:
    docker:
        api: Dockerfile
        postgres: Dockerfile.postgres
release:
    image: api
    command:
        - echo ">> Creating database"
        - python -m gatovid --create-db
run:
    api: gunicorn -w 1 -b :8000 gatovid.app:app
